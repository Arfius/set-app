import { DateDetailsPipe } from './date-details.pipe';

describe('DateDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new DateDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
